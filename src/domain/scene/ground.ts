export default class Ground {
  
    width: number;
    height: number;
    color: number;

    constructor(
        width: number,
        height: number,
        color: number
    ) {
      this.width = width;
      this.height = height;
      this.color = color;
    }

}